name: Backend CI

on:
  workflow_call:

jobs:
  lint:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: fitosanitario-backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'
          cache-dependency-path: fitosanitario-backend/package-lock.json
      - run: npm ci
      - run: npm run lint

  test:
    runs-on: ubuntu-latest
    outputs:
      test_suites: ${{ steps.capture.outputs.test_suites }}
      test_tests: ${{ steps.capture.outputs.test_tests }}
    defaults:
      run:
        working-directory: fitosanitario-backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'
          cache-dependency-path: fitosanitario-backend/package-lock.json
      - run: npm ci
      - run: npm test 2>&1 | tee /tmp/backend-test.log; exit ${PIPESTATUS[0]}
      - id: capture
        if: always()
        run: |
          echo "test_suites=$(grep -oP 'Test Suites: .+' /tmp/backend-test.log 2>/dev/null || echo 'N/A')" >> $GITHUB_OUTPUT
          echo "test_tests=$(grep -oP 'Tests: .+' /tmp/backend-test.log 2>/dev/null || echo 'N/A')" >> $GITHUB_OUTPUT
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: junit-backend
          path: fitosanitario-backend/junit.xml

  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: fitosanitario-backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'
          cache-dependency-path: fitosanitario-backend/package-lock.json
      - run: npm ci
      - run: npm run build

  notify:
    needs: [lint, test, build]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Notificar resultado Backend
        run: |
          L="${{ needs.lint.result }}"
          T="${{ needs.test.result }}"
          B="${{ needs.build.result }}"
          S="${{ needs.test.outputs.test_suites }}"
          TS="${{ needs.test.outputs.test_tests }}"

          LINEAS=""
          LINEAS="${LINEAS}🔍 Fase 1: Lint (ESLint) → $( [ \"$L\" = \"success\" ] && echo '✅ SUPERADO' || echo '❌ FALLIDO' )%0A"
          LINEAS="${LINEAS}🧪 Fase 2: Tests (Jest) → $( [ \"$T\" = \"success\" ] && echo '✅ SUPERADO' || echo '❌ FALLIDO' )%0A"
          [ -n "$S" ] && [ "$S" != "N/A" ] && LINEAS="${LINEAS}     📊 Suites: ${S}%0A"
          [ -n "$TS" ] && [ "$TS" != "N/A" ] && LINEAS="${LINEAS}     📊 Tests: ${TS}%0A"
          LINEAS="${LINEAS}📦 Fase 3: Build → $( [ \"$B\" = \"success\" ] && echo '✅ SUPERADO' || echo '❌ FALLIDO' )%0A"

          RESUMEN="✔️ ${L}/${T}/${B}"
          if [ "$L" = "success" ] && [ "$T" = "success" ] && [ "$B" = "success" ]; then
            TITULO="✅ Backend - SUPERADO"
            ESTADO="🟢"
          else
            TITULO="❌ Backend - FALLIDO"
            ESTADO="🔴"
          fi

          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
            -d text="📦 *Backend* | ${ESTADO} ${TITULO}%0A%0A${LINEAS}%0A🔗 ${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}" \
            -d parse_mode="Markdown"
